# Mock 模式：auto | 1 | 0
# auto = 自动检测（有真实服务走真实，否则 Mock）
# 1   = 强制 Mock
# 0   = 强制真实（缺服务则报错）
LOCAL_AI_MOCK=auto

# 各模型服务地址（Docker 内部通信）
ASR_URL=http://asr:8001
TTS_URL=http://tts:8004
NLP_URL=http://nlp:8005
MTRAN_URL=http://mtran:8989

# TTS 默认中文语音
# zh_CN-xiao_ya-medium = 拼音方案，停顿更自然（依赖 g2pW，约 152MB 首次下载）
# zh_CN-huayan-medium  = espeak 方案，中英混读略好
TTS_PIPER_VOICE=zh_CN-xiao_ya-medium

# 文本 AI 的工具能力（function calling）总开关
# 开着时模型可调用：当前时间、计算器、联网搜索、网页抓取
# 单个请求还能用 params.tools=false 覆盖（语音管线就是这么关的）
NLP_TOOLS=true

# 容器时区。容器默认 UTC，不设的话「现在几点」会答错 8 小时，
# gateway 的日志时间戳也会是 UTC
TZ=Asia/Shanghai

# 对外 OpenAI 兼容接口（/v1/models、/v1/chat/completions）的 API Key
# 留空 = 不校验，本地私有部署的默认。要暴露到局域网外时再填，
# 届时客户端（Cherry Studio 等）的 API Key 栏填同一个值
OPENAI_API_KEY=

# 模型下载源（国内用 hf-mirror；海外可改为 https://huggingface.co）
# 影响两处：TTS 拉取 g2pW 注音模型、Web 面板「模型管理」页的模型下载
# 注意：命令行脚本 scripts/download-models.sh 读的是另一个变量 HF_MIRROR
HF_ENDPOINT=https://hf-mirror.com
